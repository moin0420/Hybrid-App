{
  "name": "hybrid-app-backend",
  "version": "1.0.0",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "start": "node app.js",
    "build-frontend": "cd ../frontend && npm install && npm run build && mkdir -p ./frontend && cp -r build/* ../backend/frontend/",
    "deploy": "npm run build-frontend && node app.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "dotenv": "^16.3.1",
    "pg": "^8.11.1",
    "socket.io": "^4.7.2"
  },
  "engines": {
    "node": "20.x"
  }
}
